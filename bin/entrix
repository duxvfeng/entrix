#!/usr/bin/env python3
# Entrix CLI launcher script - Unix/Linux/macOS
import sys
import subprocess

if __name__ == "__main__":
    sys.exit(subprocess.run([sys.executable, "-m", "entrix"] + sys.argv[1:]).returncode)